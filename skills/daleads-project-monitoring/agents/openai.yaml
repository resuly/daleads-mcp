interface:
  display_name: "DA Leads Project Monitoring"
  short_description: "Monitor canonical development projects and lifecycle changes"
  default_prompt: "Use $daleads-project-monitoring to find this project, explain its returned normalized status and linked applications, then poll redacted future changes with the durable cursor."
