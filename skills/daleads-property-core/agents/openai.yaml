interface:
  display_name: "DA Leads Property Core"
  short_description: "Inspect verified address and parcel context safely"
  default_prompt: "Use $daleads-property-core to inspect the verified public-record context for this Australian property."
